# Supabase project config (Project Settings -> API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Google Sheets Web App URL (leads backup)
VITE_GOOGLE_SHEET_URL=

# --- WhatsApp bot (server-side only — set these in Vercel's dashboard, NEVER
# prefix with VITE_ or they'd get bundled into the public frontend) ---
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_VERIFY_TOKEN=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
